# test_nuget_interop.tkv - Test module nuget_interop.tkv
__tkv_import__ = "nuget_interop"

def main() -> "i32":
    pkg = load_external_package("System.Numerics")
    res = call_package_symbol(pkg, "Matrix4x4")
    
    if len(pkg) > 0 and res == 1:
        return 1
    return 0
