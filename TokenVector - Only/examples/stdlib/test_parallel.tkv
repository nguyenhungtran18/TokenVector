# test_parallel.tkv - Test module parallel.tkv
__tkv_import__ = "parallel"

def main() -> "i32":
    items = []
    items.append(1)
    items.append(2)
    items.append(3)
    
    res = parallel_map_i32(items)
    if len(res) == 3:
        return 1
    return 0
