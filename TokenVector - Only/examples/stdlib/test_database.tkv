# test_database.tkv - Test module database_kv.tkv
__tkv_import__ = "database_kv"

def main() -> "i32":
    db = db_create_table()
    ok1 = db_insert(db, "usr_101", "Nguyen Hung")
    ok2 = db_insert(db, "usr_102", "TokenVector Core")
    
    val = db_query(db, "usr_101")
    count = db_count_keys(db)
    
    if val == "Nguyen Hung" and count >= 2 and ok1 == 1:
        return 1
    return 0
