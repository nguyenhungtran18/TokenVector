# test_exception.tkv - Test custom exception handling

def main() -> "i32":
    caught = 0
    try:
        raise ValueError("Loi du lieu khong hop le")
    except ValueError:
        caught = 1
    
    if caught == 1:
        return 1
    return 0
