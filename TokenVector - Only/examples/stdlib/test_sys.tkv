# test_sys.tkv - Test module sys.tkv
__tkv_import__ = "sys"

def main() -> "i32":
    p = get_platform()
    v = get_version()
    
    if p == "win32_cil" and len(v) > 0:
        return 1
    return 0
