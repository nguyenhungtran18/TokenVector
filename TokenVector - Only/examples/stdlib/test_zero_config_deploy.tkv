# test_zero_config_deploy.tkv - Test module zero_config_deploy.tkv
__tkv_import__ = ["zero_config_deploy", "os"]

def main() -> "i32":
    bundle_dir = "C:\\Claude AI Project\\TokenVector\\TokenVector - Only\\examples\\stdlib\\bundle_dir"
    ok_dir = os_mkdir(bundle_dir)
    
    pkg_file = "C:\\Claude AI Project\\TokenVector\\TokenVector - Only\\examples\\stdlib\\app_bundle.zip"
    ok_pkg = create_single_file_package(bundle_dir, pkg_file)
    ok_ver = verify_bundle_integrity(pkg_file)
    
    if ok_pkg == 1 and ok_ver == 1:
        return 1
    return 0
