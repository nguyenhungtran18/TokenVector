# test_base64.tkv - Test module base64.tkv
__tkv_import__ = "base64"

def main() -> "i32":
    encoded = b64encode("TokenVector")
    decoded = b64decode(encoded)
    if decoded == "TokenVector" and len(encoded) > 0:
        return 1
    return 0
