# divmod() Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm `divmod(a, b) -> (i32,i32)` hoặc `(i64,i64)`, dùng được
qua `q, r = divmod(a, b)`, đóng mục CUỐI batch 5.5b trong
`docs/PYTHON_GAP_CHECKLIST.md`.

**Architecture:** Mở rộng `register_expr_builtin` (`il_dispatch.py`)
thêm `return_ta_fn` động (dtype phụ thuộc tham số, khác `return_ta` cố
định của `path_splitext`). Mở rộng `tuple_assign` (`tuple_type.py`,
tiếp tục đụng từ batch trước) nhận diện `return_ta_fn`. `divmod(a, b)`
trong file mới `compiler/il_features/divmod_builtin.py` — viết lại logic
floor-adjust của `_emit_int_floor_div_or_mod` (`operators.py`, đã chứng
minh đúng qua `//`/`%`) cho node `call` (khác `binop`), dùng `temps_fn=`
khai 4 local (`a`,`b`,`q`,`r`).

**Tech Stack:** Python 3 (compiler), CIL text + `ilasm.exe` (.NET
Framework mscorlib v4.0.30319).

## Global Constraints

- TUYỆT ĐỐI KHÔNG build/rebuild `release/3.code/dist/tkvc.exe` ở BẤT KỲ
  task nào trong plan này.
- Cả 2 cây `compiler/` (`.py`) và `release/3.code/compiler/` (`.tkv`)
  PHẢI sửa đồng bộ 100% ở mọi task chạm code.
- `path_splitext`'s `tuple_assign` (nhánh `EXPR_BUILTIN_RETURN_TA` cố
  định, batch trước) PHẢI giữ NGUYÊN hành vi — Task 1 chỉ THÊM nhánh
  mới, không sửa nhánh cũ.
- `//`/`%` binop hiện có (`operators.py`) PHẢI giữ NGUYÊN hành vi —
  không đụng file đó, chỉ THAM CHIẾU logic (viết lại độc lập cho
  `divmod`, không refactor dùng chung).
- Chỉ hỗ trợ `i32`/`i64` cho `divmod` — `f32`/`f64` raise lỗi biên dịch
  rõ ràng. Tham số đầu PHẢI là 1 biến đơn (giống giới hạn `sample`/
  `choice`/`shuffle`).
- Không refactor code không liên quan ngoài phạm vi các file đã liệt kê.

---

### Task 1: Mở rộng `register_expr_builtin`/`tuple_assign` hỗ trợ `return_ta_fn` động

**Files:**
- Modify: `compiler/il_dispatch.py` — thêm `EXPR_BUILTIN_RETURN_TA_FN`
  registry + tham số `return_ta_fn` vào `register_expr_builtin` (dòng
  101-133 hiện tại)
- Modify: `release/3.code/compiler/il_dispatch.tkv` (mirror)
- Modify: `compiler/il_features/tuple_type.py` (249 dòng hiện tại — sửa
  `fpw_tuple_assign` dòng 189-227, `codegen_tuple_assign` dòng 112-136)
- Modify: `release/3.code/compiler/il_features/tuple_type.tkv` (mirror)

**Interfaces:**
- Consumes: `EXPR_BUILTIN_RETURN_TA` (registry cố định có sẵn từ batch
  trước).
- Produces: `EXPR_BUILTIN_RETURN_TA_FN` registry + `return_ta_fn=`
  param trên `register_expr_builtin` — Task 2 dùng cho `divmod`.

- [ ] **Step 1: Thêm `EXPR_BUILTIN_RETURN_TA_FN` + tham số
  `return_ta_fn` vào `register_expr_builtin`**

Sửa `compiler/il_dispatch.py`, thêm dòng NGAY SAU
`EXPR_BUILTIN_RETURN_TA = {}` (dòng 88):
```python
EXPR_BUILTIN_RETURN_TA = {}
# dtype PHU THUOC tham so thuc te luc goi (khac EXPR_BUILTIN_RETURN_TA
# co dinh o tren) - vd divmod(a,b) tra (i32,i32)/(i64,i64) tuy dtype cua
# 'a' (batch 5.5b muc cuoi, 2026-08-13). fn(args, scope) -> TypeAnn|None.
EXPR_BUILTIN_RETURN_TA_FN = {}
```

Sửa chữ ký `register_expr_builtin` (dòng 101-102):
```python
def register_expr_builtin(name, codegen_fn, return_dtype, return_shape=None,
                           temps_fn=None, return_dtype_fn=None, return_ta=None,
                           return_ta_fn=None, native_int=False):
```

Thêm vào cuối thân hàm (sau khối `if return_shape is not None:` hiện
tại, dòng 129-133):
```python
    if return_ta_fn is not None:
        EXPR_BUILTIN_RETURN_TA_FN[name] = return_ta_fn
```

- [ ] **Step 2: Mirror Step 1 sang `.tkv`**

Áp dụng NGUYÊN VĂN vào `release/3.code/compiler/il_dispatch.tkv`.

- [ ] **Step 3: Sửa `fpw_tuple_assign` nhận diện `return_ta_fn`**

Sửa `compiler/il_features/tuple_type.py`, thêm import
`EXPR_BUILTIN_RETURN_TA_FN` (cạnh `EXPR_BUILTIN_RETURN_TA` đã import từ
batch trước — tìm bằng `grep -n "EXPR_BUILTIN_RETURN_TA" compiler/il_features/tuple_type.py`
để xác nhận dòng import hiện tại trước khi sửa).

Sửa nhánh `elif builtin_name in EXPR_BUILTIN_RETURN_TA and ...` (dòng
211-218 hiện tại) — THÊM 1 nhánh `elif` MỚI ngay sau nó, TRƯỚC `else:`
cuối cùng (dòng 219):

```python
        elif builtin_name in EXPR_BUILTIN_RETURN_TA and \
                EXPR_BUILTIN_RETURN_TA[builtin_name].shape == 'tuple':
            builtin_ta = EXPR_BUILTIN_RETURN_TA[builtin_name]
            if len(builtin_ta.tuple_dtypes) != len(targets):
                raise SyntaxError(
                    f"il_codegen: '{builtin_name}(...)' tra ve tuple "
                    f"{len(builtin_ta.tuple_dtypes)} phan tu, khong khop "
                    f"{len(targets)} target ben trai")
            dtypes = builtin_ta.tuple_dtypes
        elif builtin_name in EXPR_BUILTIN_RETURN_TA_FN:
            # dtype PHU THUOC tham so (vd divmod(a,b) - batch 5.5b muc
            # cuoi, 2026-08-13). Goi fn(args, infer_scope) NGAY O FIRST
            # PASS de suy dtype tu bien 'a' da khai bao truoc do.
            dyn_ta = EXPR_BUILTIN_RETURN_TA_FN[builtin_name](call_node[2], infer_scope)
            if dyn_ta is None or dyn_ta.shape != 'tuple':
                raise SyntaxError(
                    f"il_codegen: '{builtin_name}(...)' khong suy duoc kieu tra ve tuple "
                    f"(tham so dau co the chua phai 1 BIEN da khai bao kieu ro rang)")
            if len(dyn_ta.tuple_dtypes) != len(targets):
                raise SyntaxError(
                    f"il_codegen: '{builtin_name}(...)' tra ve tuple "
                    f"{len(dyn_ta.tuple_dtypes)} phan tu, khong khop "
                    f"{len(targets)} target ben trai")
            dtypes = dyn_ta.tuple_dtypes
        else:
```

**LƯU Ý**: hàm `fpw_tuple_assign` PHẢI có `infer_scope` khả dụng trong
phạm vi (kiểm tra chữ ký hàm — dòng đầu `def fpw_tuple_assign(stmt, ctx):`
— biến `infer_scope = ctx.get('infer_scope')` đã có sẵn ở đầu hàm, xem
dòng ~156 hiện tại, dùng LẠI biến đó, không khai lại).

- [ ] **Step 4: Sửa `codegen_tuple_assign` nhận diện `return_ta_fn`**

Sửa nhánh `else:` (dòng 125-127 hiện tại):
```python
        if ctx.get('func_table') and builtin_name in ctx['func_table']:
            dtypes = ctx['func_table'][builtin_name].return_type.tuple_dtypes
        else:
            from il_dispatch import EXPR_BUILTIN_RETURN_TA as _EBRT
            dtypes = _EBRT[builtin_name].tuple_dtypes
```
thành:
```python
        if ctx.get('func_table') and builtin_name in ctx['func_table']:
            dtypes = ctx['func_table'][builtin_name].return_type.tuple_dtypes
        else:
            from il_dispatch import EXPR_BUILTIN_RETURN_TA as _EBRT
            from il_dispatch import EXPR_BUILTIN_RETURN_TA_FN as _EBRTFN
            if builtin_name in _EBRT:
                dtypes = _EBRT[builtin_name].tuple_dtypes
            else:
                dtypes = _EBRTFN[builtin_name](call_node[2], scope).tuple_dtypes
```

- [ ] **Step 5: Mirror Step 3/4 sang `.tkv`**

Áp dụng NGUYÊN VĂN vào
`release/3.code/compiler/il_features/tuple_type.tkv`.

- [ ] **Step 6: Xác nhận KHÔNG regression trên `path_splitext` (nhánh
  `EXPR_BUILTIN_RETURN_TA` cố định) và hàm người dùng (`func_table`)**

```bash
cd "D:\Claude AI Project\TokenVector"
python tkv.py build release/3.code/Testkit/path_splitext_test.tkv --entry run --out "$env:TEMP/dm_t1_pse.exe"
"$env:TEMP/dm_t1_pse.exe"
python test/verify/tuple_test.py
```
Expected: `path_splitext_test` vẫn `SUMMARY 6/6`; `tuple_test.py` vẫn
PASS toàn bộ (8/8 theo lần chạy trước).

- [ ] **Step 7: Commit**

```bash
cd "D:\Claude AI Project\TokenVector"
git add compiler/il_dispatch.py release/3.code/compiler/il_dispatch.tkv \
        compiler/il_features/tuple_type.py \
        release/3.code/compiler/il_features/tuple_type.tkv
git commit -m "$(cat <<'EOF'
feat(compiler): register_expr_builtin them return_ta_fn dong cho tuple

EXPR_BUILTIN_RETURN_TA_FN moi - dtype tra ve PHU THUOC tham so thuc te
(khac EXPR_BUILTIN_RETURN_TA co dinh cua path_splitext). tuple_assign
(fpw_tuple_assign/codegen_tuple_assign) them nhanh tra registry moi nay
TRUOC khi raise loi cuoi cung - giu nguyen 2 nhanh cu (func_table +
return_ta co dinh). Chuan bi cho divmod(a,b) - dtype tra ve tuy dtype
cua 'a'.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `divmod(a, b)` + test + regression + docs

**Files:**
- Create: `compiler/il_features/divmod_builtin.py`
- Create: `release/3.code/compiler/il_features/divmod_builtin.tkv`
- Test: `release/3.code/Testkit/divmod_test.tkv` (MỚI)
- Modify: `docs/PYTHON_GAP_CHECKLIST.md`
- Modify: bất kỳ file `plugin_loader.py`/danh sách module nạp động nào
  liệt kê rõ tên file `il_features/*.py` (kiểm tra bằng
  `grep -rn "il_features" tkv_compile.py compiler/*.py | grep -i "listdir\|glob\|import_module"`
  — nếu module nạp ĐỘNG theo thư mục (không cần liệt kê tên file), bỏ
  qua bước này; nếu có danh sách tường minh, thêm `divmod_builtin` vào
  đó ở CẢ 2 cây)

**Interfaces:**
- Consumes: `EXPR_BUILTIN_RETURN_TA_FN`/`tuple_assign` mở rộng từ Task
  1.
- Produces: không có interface mới cho task khác — task đóng gói cuối
  của TOÀN BỘ batch 5.5b.

- [ ] **Step 1: Xác nhận file mới được nạp đúng (module discovery)**

```bash
cd "D:\Claude AI Project\TokenVector"
grep -n "il_features" tkv_compile.py | grep -i "import\|listdir\|glob"
```
Nếu `il_codegen.py`/`tkv_compile.py` import CỐ ĐỊNH từng file
`il_features/*.py` theo tên (không phải quét thư mục động), thêm dòng
import `divmod_builtin` vào đúng vị trí (giữa các import khác, theo thứ
tự alphabet hiện có nếu có quy ước) — NẾU nạp động qua
`plugin_loader.py`/quét thư mục, bỏ qua bước này (file mới tự được nạp).

- [ ] **Step 2: Viết `compiler/il_features/divmod_builtin.py`**

```python
# -*- coding: utf-8 -*-
"""divmod(a, b) -> (i32,i32)/(i64,i64) tuy dtype cua 'a' (batch 5.5b muc
cuoi, 2026-08-13). Viet lai logic floor-adjust cua operators.py's
_emit_int_floor_div_or_mod (DA CHUNG MINH DUNG qua toan tu '//'/'%') -
KHONG tai dung truc tiep duoc vi divmod la node 'call' (khong phai
'binop'), nen khong di qua co che _walk_intdiv_nodes/'__idiv{id(node)}'
(khoa theo id() cua node 'binop'). Dung temps_fn= (giong path_splitext/
sample) thay the.

GIOI HAN DA BIET: chi ho tro i32/i64 (f32/f64 bao loi ro rang - Python
divmod() tren so thuc dung ngu nghia khac, ngoai pham vi). Tham so DAU
phai la 1 BIEN don da khai bao (giong sample/choice/shuffle) - dtype suy
THANG tu do. b=0 de CIL tu nem DivideByZeroException (khong tu viet
message rieng, chap nhan khac ZeroDivisionError cua Python that)."""
from typed_dsl_parser import TypeAnn
from il_features.tuple_type import il_tupleN_type
from il_dispatch import register_expr_builtin

_SUPPORTED_DTYPES = ('i32', 'i64')


def _divmod_dtype(args, scope):
    """Suy dtype cua divmod(a,b) tu bien 'a' - tra None neu khong suy
    duoc (khong phai loi NGAY, de caller tu quyet dinh raise ro rang)."""
    if len(args) != 2 or args[0][0] != 'var':
        return None
    try:
        return scope[args[0][1]][2].dtype
    except KeyError:
        return None


def _divmod_return_ta_fn(args, scope):
    d = _divmod_dtype(args, scope)
    if d is None or d not in _SUPPORTED_DTYPES:
        return None
    return TypeAnn(d, 'tuple', tuple_dtypes=[d, d])


def _divmod_temps(node, ctx):
    args = node[2]
    if len(args) != 2:
        return
    d = _divmod_dtype(args, ctx['infer_scope']) or 'i32'
    TypeAnn_ = ctx['TypeAnn']
    for suf in ('a', 'b', 'q', 'r'):
        ctx['declare_named'](f'__dm{id(args)}_{suf}', TypeAnn_(d, None))


def compile_divmod(args, scope, out, dtype, ctx):
    if len(args) != 2:
        raise SyntaxError("il_codegen: divmod(a, b) chi nhan dung 2 tham so")
    d = _divmod_dtype(args, scope)
    if d is None:
        raise SyntaxError(
            "il_codegen: divmod(a, b) - 'a' phai la 1 BIEN da khai bao kieu "
            "i32/i64 (dtype suy tu 'a')")
    if d not in _SUPPORTED_DTYPES:
        raise SyntaxError(
            f"il_codegen: divmod(a, b) chi ho tro i32/i64, khong ho tro '{d}'")
    compile_expr = ctx['compile_expr']
    a_idx = scope[f'__dm{id(args)}_a'][1]
    b_idx = scope[f'__dm{id(args)}_b'][1]
    q_idx = scope[f'__dm{id(args)}_q'][1]
    r_idx = scope[f'__dm{id(args)}_r'][1]

    zero = '    ldc.i4.0' if d == 'i32' else '    ldc.i8 0'
    one = '    ldc.i4.1' if d == 'i32' else '    ldc.i8 1'

    compile_expr(args[0], scope, out, d, ctx)
    out.append(f'    stloc.s {a_idx}')
    compile_expr(args[1], scope, out, d, ctx)
    out.append(f'    stloc.s {b_idx}')
    out.append(f'    ldloc.s {a_idx}')
    out.append(f'    ldloc.s {b_idx}')
    out.append('    div')
    out.append(f'    stloc.s {q_idx}')
    out.append(f'    ldloc.s {a_idx}')
    out.append(f'    ldloc.s {b_idx}')
    out.append('    rem')
    out.append(f'    stloc.s {r_idx}')

    ctx['label_counter'][0] += 1
    n = ctx['label_counter'][0]
    prefix = ctx.get('prefix', 'dm')
    done_lbl = f'{prefix}_dm{n}_done'

    out.append(f'    ldloc.s {r_idx}')
    out.append(zero)
    out.append(f'    beq {done_lbl}')
    out.append(f'    ldloc.s {a_idx}')
    out.append(f'    ldloc.s {b_idx}')
    out.append('    xor')
    out.append(zero)
    out.append(f'    bge {done_lbl}')
    out.append(f'    ldloc.s {q_idx}')
    out.append(one)
    out.append('    sub')
    out.append(f'    stloc.s {q_idx}')
    out.append(f'    ldloc.s {r_idx}')
    out.append(f'    ldloc.s {b_idx}')
    out.append('    add')
    out.append(f'    stloc.s {r_idx}')
    out.append(f'  {done_lbl}:')

    out.append(f'    ldloc.s {q_idx}')
    out.append(f'    ldloc.s {r_idx}')
    tuple_type = il_tupleN_type([d, d])
    out.append(f'    newobj instance void {tuple_type}::.ctor(!0, !1)')


register_expr_builtin('divmod', compile_divmod, None,
                       temps_fn=_divmod_temps,
                       return_ta_fn=_divmod_return_ta_fn)
```

**LƯU Ý thứ tự operand `xor`/`sub`/`add`**: đối chiếu LẠI với
`_emit_int_floor_div_or_mod` (`compiler/il_features/operators.py`, hàm
gốc đã chứng minh đúng) — code trên COPY gần như nguyên xi logic đó
(chỉ khác tính CẢ q lẫn r cùng lúc thay vì chọn 1 theo `op`). Nếu build
test Step 6 cho sai kết quả với số âm, đối chiếu lại TỪNG dòng với hàm
gốc trước khi tự đoán sửa.

- [ ] **Step 3: Mirror sang `.tkv`**

Tạo `release/3.code/compiler/il_features/divmod_builtin.tkv` với nội
dung NGUYÊN VĂN (chuyển `_` giữ nguyên, không đổi tên file — kiểm tra
quy ước đặt tên `.tkv` hiện có của các file `.py` khác để xác nhận
đúng, thường CÙNG TÊN chỉ đổi đuôi).

- [ ] **Step 4: Viết test mới `divmod_test.tkv`**

Tạo `release/3.code/Testkit/divmod_test.tkv`:

```python
__tkv_import__ = ["tkv_test_lib"]

def run() -> "i32":
    total = 0
    tested = 0

    a1 = 17
    b1 = 5
    q1, r1 = divmod(a1, b1)
    tested = tested + 1
    total = total + check("divmod_pos_q", str(q1), "3")
    tested = tested + 1
    total = total + check("divmod_pos_r", str(r1), "2")

    a2 = -7
    b2 = 2
    q2, r2 = divmod(a2, b2)
    tested = tested + 1
    total = total + check("divmod_neg_a_q", str(q2), "-4")
    tested = tested + 1
    total = total + check("divmod_neg_a_r", str(r2), "1")

    a3 = 7
    b3 = -2
    q3, r3 = divmod(a3, b3)
    tested = tested + 1
    total = total + check("divmod_neg_b_q", str(q3), "-4")
    tested = tested + 1
    total = total + check("divmod_neg_b_r", str(r3), "-1")

    a4 = -7
    b4 = -2
    q4, r4 = divmod(a4, b4)
    tested = tested + 1
    total = total + check("divmod_both_neg_q", str(q4), "3")
    tested = tested + 1
    total = total + check("divmod_both_neg_r", str(r4), "-1")

    return test_summary("divmod_test", total, tested)
```

**LƯU Ý**: 4 ca trên đối chiếu TRỰC TIẾP với `divmod()` Python THẬT
(`python3 -c "print(divmod(17,5), divmod(-7,2), divmod(7,-2), divmod(-7,-2))"`)
TRƯỚC khi tin giá trị mong đợi viết sẵn ở trên — nếu Python thật cho
khác, SỬA lại test theo Python thật, không giữ nguyên giá trị đoán.

- [ ] **Step 5: Build + chạy thật, xác nhận PASS**

```bash
cd "D:\Claude AI Project\TokenVector"
python tkv.py build release/3.code/Testkit/divmod_test.tkv --entry run --out "$env:TEMP/dm_t2.exe"
"$env:TEMP/dm_t2.exe"
```
Expected: build PASS, `SUMMARY 8/8`.

- [ ] **Step 6: Regression toàn bộ `Testkit/*.tkv` qua cây `.py`**

```bash
cd "D:\Claude AI Project\TokenVector"
for f in release/3.code/Testkit/*.tkv; do
  base=$(basename "$f" .tkv)
  case "$base" in tkv_test_lib|import_lib_mod|example_lib|input_py_tree_test|native_test_suite) continue;; esac
  python tkv.py build "$f" --entry run --out "$TEMP/dm_reg_${base}.exe" > "$TEMP/dm_buildlog_${base}.log" 2>&1
  if [ $? -ne 0 ]; then echo "BUILD-FAIL $base"; continue; fi
  res=$("$TEMP/dm_reg_${base}.exe" 2>&1)
  echo "$res" | grep -qi "^FAIL \|Exception" && { echo "=== $base ==="; echo "$res" | tail -5; } || echo "OK $base"
done
```

Expected: mọi dòng `OK` trừ `path_isfile_isdir_test` (pre-existing fail
đã biết, không liên quan).

- [ ] **Step 7: Cập nhật `docs/PYTHON_GAP_CHECKLIST.md` — đóng TOÀN BỘ
  batch 5.5b**

Đọc lại nội dung THẬT của dòng `5.5b batch nhỏ còn lại` (hiện chỉ còn
`divmod()`, `set.remove()`). Tách `divmod()` thành `[x]` riêng. Vì đây
là mục CUỐI CÙNG của batch 5.5b trừ `set.remove()`, dòng `5.5b batch
nhỏ còn lại` giờ chỉ còn 1 mục:

```
- [x] `divmod(a, b)` — **ĐÃ XONG (2026-08-13)**. `divmod(a,b) ->
      (i32,i32)/(i64,i64)` qua vòng lặp floor-adjust (viết lại logic đã
      có của `//`/`%`). Mở rộng `register_expr_builtin` thêm
      `return_ta_fn` động (dtype phụ thuộc tham số). Giới hạn: chỉ
      i32/i64, tham số đầu phải là 1 biến đơn. Xem
      `docs/superpowers/specs/2026-08-13-divmod-design.md`.
- [ ] 5.5b batch nhỏ còn lại: `set.remove()` phải ném lỗi khi thiếu
      phần tử
```

(Đọc lại nội dung THẬT của file trước khi sửa — không giả định đúng
format trên nếu khác thực tế.)

- [ ] **Step 8: Commit**

```bash
cd "D:\Claude AI Project\TokenVector"
git add compiler/il_features/divmod_builtin.py \
        release/3.code/compiler/il_features/divmod_builtin.tkv \
        release/3.code/Testkit/divmod_test.tkv \
        docs/PYTHON_GAP_CHECKLIST.md
git commit -m "$(cat <<'EOF'
feat(compiler): divmod(a, b) -> (i32,i32)/(i64,i64)

Viet lai logic floor-adjust cua operators.py's _emit_int_floor_div_or_mod
(da chung minh dung qua toan tu //,% ) cho node 'call' - tinh CA q LAN r
cung luc thay vi toggle theo op. Dung return_ta_fn dong (Task 1) de dtype
tra ve tuy dtype cua 'a'. Test moi doi chieu Python that (a/b duong, a am,
b am, ca 2 am). Regression toan bo Testkit/*.tkv - khong hoi quy moi.
Danh dau divmod() xong trong checklist 5.5b - chi con set.remove().

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```
